{
    "name": "quoc-thai/cpu-scheduling",
    "description": "CPU Scheduling Algorithms Implementation",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Your Name",
            "email": "your.email@example.com"
        }
    ],
    "require": {
        "php": "^8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5",
        "squizlabs/php_codesniffer": "^3.6"
    },
    "scripts": {
        "test": "phpunit",
        "check-style": "phpcs -p --standard=PSR12 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit 1 src tests",
        "fix-style": "phpcbf -p --standard=PSR12 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit 1 src tests"
    },
    "autoload": {
        "psr-4": {
            "CPUScheduling\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "CPUScheduling\\Tests\\": "tests/"
        }
    }
}